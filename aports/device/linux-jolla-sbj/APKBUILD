# APKBUILD based on linux-vanilla aport. Changes:
# - disabled module installation
# - add !check !tracedeps
# - package: just install zimage and kernel.release, because the kernel config
#	does not generate modules or dtb files
# - do not create -dev subpackage (makes no sense without module support)
#
# Kernel config based on: arch/arm/configs/lineageos_mako_defconfig
# Changes:
# - enable devtmpfs (needed for udev -> touch support in weston)

_vendor=jolla
_flavor=jolla-sbj
_hash="2.0.5.6"
_config="config-${_flavor}.armhf"

pkgname=linux-${_flavor}
pkgver=3.4.0
case $pkgver in
	*.*.*)  _kernver=${pkgver%.*};;
	*.*) _kernver=$pkgver;;
esac
pkgrel=3
arch="armhf"
pkgdesc="Jolla phone kernel from djselbeck with opensource wlan drivers"
url="https://github.com/djselbeck/sailfishos_kernel_jolla_msm8930/archive/7a081f367ac1fc006ab1e4b0d63aa5ea7bea8a70.zip"
depends="postmarketos-mkinitfs"
makedepends="perl sed installkernel bash gmp-dev bc linux-headers elfutils-dev"
options="!strip !check !tracedeps"
install=
source="
	$pkgname-$_hash.zip::https://github.com/djselbeck/sailfishos_kernel_jolla_msm8930/archive/7a081f367ac1fc006ab1e4b0d63aa5ea7bea8a70.zip
	$_config
	compiler-gcc6.h
	duplicate-return-address-definition.patch
	kgsl-iommu-locks.patch
"
subpackages=""
license="GPL2"

_abi_release=${pkgver}
_carch="arm"
HOSTCC="${CC:-gcc}"
HOSTCC="${HOSTCC#${CROSS_COMPILE}}"

kver="sailfishos_kernel_jolla_msm8930"
ksrcdir="$srcdir/$kver"
kernelarchive="$srcdir/$pkgname-$_hash.zip"

prepare() {
	local _patch_failed=
	cd "$srcdir"
	unzip -n "$kernelarchive"
	cd "$ksrcdir"

	# first apply patches in specified order
	for i in $source; do
		case $i in
		*.patch)
			msg "Applying $i..."
			if ! patch -s -p1 -N -i "$srcdir"/$i; then
				echo $i >>failed
				_patch_failed=1
			fi
			;;
		esac
	done

	if ! [ -z "$_patch_failed" ]; then
		error "The following patches failed:"
		cat failed
		return 1
	fi

	# gcc6 support
	 cp -v "$srcdir/compiler-gcc6.h" "$ksrcdir/include/linux/"

	mkdir -p "$srcdir"/build

	msg "srcdir  = $srcdir"
	msg "ksrcdir = $ksrcdir"
	
	rm "$srcdir"/build/.config || return 1
	cp -v "$srcdir"/$_config "$srcdir"/build/.config || return 1
	make -C "$ksrcdir" O="$srcdir"/build ARCH="$_carch" HOSTCC="$HOSTCC" \
		silentoldconfig
}

# this is so we can do: 'abuild menuconfig' to reconfigure kernel
menuconfig() {
	cd "$srcdir"/build || return 1
	make ARCH="$_carch" menuconfig
	cp .config "$startdir"/$_config
}

build() {
	cd "$srcdir"/build
	unset LDFLAGS
	make ARCH="$_carch" CC="${CC:-gcc}" \
		KBUILD_BUILD_VERSION="$((pkgrel + 1 ))-Alpine" \
		|| return 1
}

package() {
	install -Dm644 "$srcdir/build/arch/arm/boot/zImage" \
		"$pkgdir/boot/vmlinuz-$_flavor"

	install -D "$srcdir/build/include/config/kernel.release" \
		"$pkgdir/usr/share/kernel/$_flavor/kernel.release"
}

sha512sums="c600ca98da22f422898f3fe9e337dea67b4dd75922c447180166cba412aad1bd708fb76dc4b89b351d44c40080442f99765552949700b9d3bd173268417f11e5  linux-jolla-sbj-2.0.5.6.zip
b51793293dd26b8fb523b52f27f9e34f164a91b417a3d06b5019af635fda04dafb2bb9aa2b6681d93f6eb7606a8cd6ba1e6fecb8557c0d1bca2a1391a8134bf4  config-jolla-sbj.armhf
d80980e9474c82ba0ef1a6903b434d8bd1b092c40367ba543e72d2c119301c8b2d05265740e4104ca1ac5d15f6c4aa49e8776cb44264a9a28dc551e0d1850dcc  compiler-gcc6.h
ea1d3b5a234fa565e3c1a792de48f4fc4e6023d281d303c8e319c7ef28edc5739ab0e4dea0139a41f0a5c7d03e27921ccaa214fd0ac5c72245a094ce60128864  duplicate-return-address-definition.patch
63d18b49c7311fe60e52192dda9a420259e173110ef99a2743db99fa550c9c2b3089f338d65b98c0b825987abbd2eb601c47b03afc835c340256c0dbf1023745  kgsl-iommu-locks.patch"
